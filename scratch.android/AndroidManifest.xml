<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.itesm.scratch.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <!-- In order for your application to use the OpenGL ES 2.0 API, you must add 
    the following declaration to your manifest: -->
    <uses-feature 
        android:glEsVersion="0x00020000" 
        android:required="true" />
    <!-- End Comment -->
    
   	
    
    <!-- If your application uses texture compression, you must also declare which 
    compression formats you support so that devices that do not support theses formats 
    do not try to run your application: -->
    <supports-gl-texture 
        android:name="GL_OES_compressed_ETC1_RGB8_texture" />
	<!-- <supports-gl-texture android:name="GL_OES_compressed_paletted_texture" /> -->
	<!-- End Comment -->
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:hardwareAccelerated="true">
        <activity
            android:name="edu.itesm.scratch.android.ScratchActivity"
            android:label="@string/title_activity_scratch" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>